// Exlaining the difference between:
// alert, prompt, and confirm.

alert("Hello!"); // <= Alerts the user via browser pop-up windows.
let prompt_value = prompt("Insert a line: "); // Prompts user for input also via pop-up windows.
confirm(); // <= Shows user pop-up with okay and cancel options to either continue or interrupt.
